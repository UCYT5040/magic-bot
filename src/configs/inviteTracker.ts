import {ConfigPart, InputConfigPart, SelectConfigPart} from "../config";
import {PrismaClient} from "../../generated/prisma";

const prisma = new PrismaClient();

// Nothing to configure for invite tracker

// export const inviteTrackerConfigParts: ConfigPart[] = [];

// export async function loadStarboardConfig(guildId: string) {}

// export async function saveStarbordConfig(guildId: string, config: any) {}